# Optional: enables the Groq-backed fluency signal. Without it, the app uses the
# documented deterministic semantic proxy so the coursework demo remains runnable.
GROQ_API_KEY=your_groq_api_key_here
