"""Detection-signal stubs for Provenance Guard."""


def calculate_predictability_score(text: str) -> float:
    """Return text predictability as a normalized score from 0.0 to 1.0.

    A lower score represents highly predictable text; a higher score represents
    highly unpredictable text. This placeholder will be replaced with the
    first detection signal's implementation.
    """
    return 0.50
